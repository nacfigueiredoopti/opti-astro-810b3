query contentByPath($url: String!, $urlNoSlash: String!) {
    _Content(
        where: {
            _or: [
                { _metadata: { url: { default: { eq: $url } } } }
                { _metadata: { url: { default: { eq: $urlNoSlash } } } }
                { _metadata: { url: { hierarchical: { eq: $url } } } }
            ]
        }
    ) {
        item {
            _metadata {
                key
                version
                locale
            }
        }
    }
}
